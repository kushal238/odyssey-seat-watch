name: fast-check-experiment
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - env:
          AMC_COOKIES: ${{ secrets.AMC_COOKIES }}
        run: python3 fast_check.py
